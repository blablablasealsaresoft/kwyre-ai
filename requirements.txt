# Kwyre AI — inference server dependencies
torch>=2.6.0
transformers>=5.3.0
bitsandbytes>=0.49.0
accelerate>=1.11.0
psutil>=7.1.0
huggingface_hub>=1.6.0
safetensors>=0.6.0
tokenizers>=0.22.0
autoawq>=0.2.0

# QAT training dependencies (not required for inference only)
peft>=0.18.0
datasets>=4.6.0
trl>=0.29.0

# Utilities
cryptography>=44.0.0
pyyaml>=6.0.0
scipy>=1.14.0,<1.16.0
